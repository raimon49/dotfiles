#!/bin/sh
[ -d "${HOME}/local/bin" ] && \
[ -d "${HOME}/.zsh/cache" ] && \
[ -d "${HOME}/.config/nvim" ] && \
[ -d "${HOME}/.config/git" ] && \
[ -d "${HOME}/.config/Code/User" ] && \
echo "OK: make directories"
